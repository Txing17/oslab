1、	函数的参数包含在a0-a7寄存器中，13被存在a2寄存器
2、	编译器优化后，并没有函数调用，g(x)被内联到f(x中，f(x)被内联到main()中，所有并没有函数的跳转。
3、	ra=pc=0x30, 1578(ra)=0x62a+030=0x65a 所以printf函数的地址为0x630
4、	ra=pc+4=0x38
5、	输出He110 World，如果是大端序，需要将i以16进制的方式逆转，即i为0x726c6400，57616不用改变。
6、	将会输出a2寄存器的值。Printf需要三个参数，但只接收了两个参数，最后一个参数位于a2寄存器，所有输出的值为a2寄存器中的值。

